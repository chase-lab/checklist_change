source("renv/activate.R")

if (Sys.info()["user"] == "as80fywe") data.table::setDTthreads(6) # this sets (and in this case increases) the number of cores data.table can use for its parallelised operations such as reading, writing and many other functionalities.

message("n*** Successfully loaded Homogenisaion Project .Rprofile ***n")
