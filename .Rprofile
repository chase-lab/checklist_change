source("renv/activate.R")

if (Sys.info()["user"] == "as80fywe") data.table::setDTthreads(8) # this sets (and in this case increases) the number of cores data.table can use for its parallelised operations such as reading, writing and many other functionalities.

message("*** Successfully loaded Homogenisaion Project .Rprofile ***")
